apiVersion: v1
kind: Service
metadata:
  name: agentic-cicd-service
  labels:
    app: agentic-cicd
spec:
  type: NodePort
  selector:
    app: agentic-cicd
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30007
